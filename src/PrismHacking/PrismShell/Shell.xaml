<Window x:Class="PrismShell.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrismShell"
        xmlns:prism="http://prismlibrary.com/"   
        mc:Ignorable="d"
        Title="Shell" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <ContentControl Grid.Row="0" prism:RegionManager.RegionName="Region1">
            
        </ContentControl>
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="Region2">

        </ContentControl>
        <ContentControl Grid.Row="2" prism:RegionManager.RegionName="Region3">

        </ContentControl>
    </Grid>
</Window>
